version: 1

AppDir:
  path: ./AppDir

  app_info:
    id: net.greenbone.openvas-deployment
    name: openvas-deployment
    icon: utilities-terminal
    version: 0.0.6
    exec: bin/bash
    exec_args: "$APPDIR/usr/bin/enterprise-container.sh $@"

  apt:
    arch: amd64
    sources:
      - sourceline: 'deb [arch=amd64] http://deb.debian.org/debian trixie main'
        key_url: 'https://ftp-master.debian.org/keys/archive-key-13.asc'
      - sourceline: 'deb [arch=amd64] https://download.docker.com/linux/debian trixie stable'
        key_url: 'https://download.docker.com/linux/debian/gpg'
    include:
      - dash
      - perl
      - make
      - bash
      - coreutils
      - ca-certificates
      - curl
      - grep
      - sed
      - gawk
      - less
      - tar
      - openssl
      - docker-ce-cli
      - docker-compose-plugin
      - oras

  runtime:
    path_mappings:
      - /usr/bin/:$APPDIR/usr/bin/
      - /usr/sbin/:$APPDIR/usr/sbin/
      - /etc/ssl/:$APPDIR/etc/ssl/
    env:
      PATH: '$APPDIR/usr/bin:$APPDIR/usr/sbin:$PATH'
      APPDIR_LIBRARY_PATH: '$APPDIR/usr/lib/x86_64-linux-gnu:$APPDIR/lib/x86_64-linux-gnu'

AppImage:
  update-information: None
  arch: x86_64
